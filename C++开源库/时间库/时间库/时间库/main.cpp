#include <iostream>
#include <chrono>
using namespace std;

int main(void)
{
	//输出当前系统范畴实时时钟的挂钟时间
	//cout << chrono::system_clock::now() << endl;
	//chrono::duration<double> diff= chrono::system_clock::now()
	return 0;
}